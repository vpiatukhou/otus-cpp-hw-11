#This script is not used by the program.
#It was used to create the DB structure.

CREATE TABLE a(id INTEGER PRIMARY KEY, name VARCHAR(100));
CREATE TABLE b(id INTEGER PRIMARY KEY, name VARCHAR(100));